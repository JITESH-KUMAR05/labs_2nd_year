#include<stdio.h>
#include<stdlib.h>
struct*Node 
{
	create node *root;
	create node *left;
	create node *right;
};

	struct Node*new Node=create Node*malloc(sizeof(struct Node))
	{
	struct Node->root=data;
	struct Node->left=NULL;
	struct Node->right=NULL;
}
if(!root)
{
	inorder traversal->root=left;
	printf("%d",root);
	inorder traversal->root=right;
}
int main()
{
	struct*create root Node=create Node(1);
	create left Node=create Node(2);
	create right Node=create Node(3);
	create left->left Node=create Node(4);
	create left->right Node=create Node(5);
	printf("inorder tree traversal \n");
	inorder traversal(root);
	return 0;
}
